# Context: Notification

## Purpose

Sends SMS, push, and email notifications to customers. Handles rate limiting, deduplication, channel routing, and delivery fallback (SMS if push failed).

## Domain Type

`generic`
